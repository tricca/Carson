# App Key della Dropbox App (dropbox.com/developers/apps).
# Non è un segreto (il login usa PKCE, senza client secret) ma tenerlo fuori da git
# evita di doverlo cambiare se in futuro si crea una nuova app Dropbox per un altro scopo.
VITE_DROPBOX_APP_KEY=
